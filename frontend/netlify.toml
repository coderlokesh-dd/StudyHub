# Netlify build configuration for StudyHub Frontend
[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — all routes go to index.html (needed for React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
